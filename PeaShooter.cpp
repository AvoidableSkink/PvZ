#include "PeaShooter.hpp"

PeaShooter::PeaShooter() { //just kinda picked, sure well change em later
    interval = 3;
    damage = 1;
    maxHealth = 8;
    currentHealth = maxHealth;
}

void shoot() {}
